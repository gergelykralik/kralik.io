extends base
block title
  title= (page.title ? page.title + " | " : "") + config.title

block main
  if page.title
    .wrapper
      .container.post-header
        h1: = page.title

  article
    .container.post
      != page.content
    if page.comments
      .container
        hr
        include _partial/comments
